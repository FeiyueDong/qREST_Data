#pragma once

#include <string_view>

namespace qrest::dynamics
{

inline constexpr int version_major = ${VERSION_MAJOR};
inline constexpr int version_minor = ${VERSION_MINOR};
inline constexpr int version_patch = ${VERSION_ALTER};
inline constexpr std::string_view version_string{"${VERSION}"};

} // namespace qrest::dynamics
